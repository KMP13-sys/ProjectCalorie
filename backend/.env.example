# Environment Configuration Template
# Copy this file to .env for local development
# For production deployment (Railway/Render), set these in their Dashboard

# Server Configuration
PORT=4000

# Database Configuration
# Local Development:
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password_here
DB_NAME=calories_app
DB_PORT=3306

# Production Deployment:
# - Railway: Use Railway's MySQL plugin (auto-inject via ${{MySQL.xxx}})
# - Render: Use render.yaml config (auto-inject from database service)

# Security Keys (IMPORTANT: Use strong random values in production!)
SECRET_KEY=your-strong-secret-key-here
JWT_SECRET=your-strong-jwt-secret-here

# Additional environment variables:
# NODE_ENV=production
# FRONTEND_URL=https://your-frontend-url.com